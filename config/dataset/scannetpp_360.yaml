defaults:
  - base_dataset
  - view_sampler: boundedv2
  - optional view_sampler_dataset_specific_config@view_sampler: boundedv2_scannetpp

name: scannetpp
roots: [datasets/scannetpp/dslr, datasets/scannetpp/iphone]

input_image_shape: [256, 256]
original_image_shape: [720, 960]
cameras_are_circular: false

baseline_min: 1e-3
baseline_max: 1e2
max_fov: 120.0

skip_bad_shape: false
